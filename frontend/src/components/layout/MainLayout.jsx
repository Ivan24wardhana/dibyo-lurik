/**
 * MainLayout
 * Layout utama (sidebar + header + content)
 */
export default function MainLayout() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold text-gray-700">MainLayout</h2>
      <p className="text-sm text-gray-500">Layout utama (sidebar + header + content)</p>
    </div>
  )
}
